<?
include_once("../common.php");


/*
    if ($member[mb_level] < 2)
    {
        if ($member[mb_id])
            alert("접근권한이  없습니다.", $g4[path]);
        else
            alert("접근 권한이 없습니다.\\n\\회원이시라면 관리자에게 문의해 보십시오.", "../bbs/login.php?wr_id=$wr_id{$qstr}&url=".urlencode($cm_url));
    }
*/

?>

